<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <layer-list>
            <item>
                <shape android:shape="rectangle">

                    <!-- view background color -->
                    <solid android:color="@color/white" />

                    <!-- view border color and width -->
                    <stroke android:width="2dp" android:color="@color/pink" />

                    <!-- If you want to add some padding -->

                    <!-- Here is the corner radius -->
                    <corners android:topLeftRadius="0dp"
                        android:topRightRadius="0dp"
                        android:bottomLeftRadius="5dp"
                        android:bottomRightRadius="5dp"/>

                </shape>
            </item>
            <!--<item>
                <bitmap android:gravity="top|right" android:src="@drawable/dropdown_icon" />
            </item>-->
        </layer-list>
    </item>
</selector>

<!--
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/navy_blue"/>
            <corners android:radius="@dimen/twenty_dp" />
            <stroke
                android:width="@dimen/twenty_dp"
                android:color="@color/navy_blue"
                />
        </shape>
    </item>
</selector>-->
